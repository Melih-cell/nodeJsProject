const circleArea = (x) => {
    console.log(`Daire Alan: ${Math.PI * x * x}`)
}

const circleCircumference = (x) => {
    console.log(`Daire çevre: ${Math.PI * x * 2}`)
}
    
    module.exports = {
       circleArea,
       circleCircumference
    }